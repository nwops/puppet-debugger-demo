source ENV['GEM_SOURCE'] || "https://rubygems.org"

group :development, :test do
  gem 'rspec', '~> 2.11',       :require => false
  gem 'beaker-rspec', '>= 3.0', :require => false
  gem 'beaker', '>= 1.20.1',    :require => false
end
